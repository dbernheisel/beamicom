defmodule Beamicom.SNES do
  @moduledoc """
  Dependency-free native Super NES emulator foundation.

  Start with `Beamicom.SNES.Machine.load/2` for an executable machine or
  `Beamicom.SNES.Cartridge.load/1` to inspect and map cartridge media without
  starting the CPU. The core is under bring-up and does not produce A/V yet;
  see the package README for the currently implemented boundary.
  """
end
