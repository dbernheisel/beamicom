--- a/c_src/exla/exla.cc
+++ b/c_src/exla/exla.cc
@@ -525,8 +525,12 @@
 FINE_NIF(get_tpu_client, 0);
 
 fine::ResourcePtr<ExlaClient> get_c_api_client(ErlNifEnv *env,
-                                               std::string device_type) {
-  return unwrap(GetCApiClient(device_type));
+                                               std::string device_type, double memory_fraction, bool preallocate) {
+  try {
+    return unwrap(GetCApiClient(device_type, memory_fraction, preallocate));
+  } catch (const std::exception &e) {
+    throw std::runtime_error(std::string("PJRT client creation: ") + typeid(e).name() + ": " + e.what());
+  }
 }
 
 FINE_NIF(get_c_api_client, 0);
--- a/c_src/exla/exla_client.cc
+++ b/c_src/exla/exla_client.cc
@@ -582,9 +582,9 @@
 }
 
 tsl::StatusOr<fine::ResourcePtr<ExlaClient>>
-GetCApiClient(std::string device_type) {
+GetCApiClient(std::string device_type, double memory_fraction, bool preallocate) {
   EXLA_ASSIGN_OR_RETURN(std::unique_ptr<xla::PjRtClient> client,
-                        xla::GetCApiClient(device_type));
+                        xla::GetCApiClient(device_type, {{"platform_name", std::string("ROCM")}, {"allocator", std::string("bfc")}, {"memory_fraction", static_cast<float>(memory_fraction)}, {"preallocate", preallocate}}));
 
   return fine::make_resource<ExlaClient>(std::move(client));
 }
--- a/c_src/exla/exla_client.h
+++ b/c_src/exla/exla_client.h
@@ -144,7 +144,7 @@
 tsl::StatusOr<fine::ResourcePtr<ExlaClient>> GetTpuClient();
 
 tsl::StatusOr<fine::ResourcePtr<ExlaClient>>
-GetCApiClient(std::string device_type);
+GetCApiClient(std::string device_type, double memory_fraction, bool preallocate);
 } // namespace exla
 
 #endif
--- a/lib/exla/client.ex
+++ b/lib/exla/client.ex
@@ -204,7 +204,7 @@
                       "shared library (for example the tt-xla pjrt_plugin_tt.so)"
 
           :ok = EXLA.NIF.load_pjrt_plugin(device_type, plugin_path)
-          EXLA.NIF.get_c_api_client(device_type)
+          EXLA.NIF.get_c_api_client(device_type, memory_fraction, preallocate)
 
         _ ->
           raise ArgumentError, "unknown EXLA platform: #{inspect(platform)}"
--- a/lib/exla/nif.ex
+++ b/lib/exla/nif.ex
@@ -3,7 +3,7 @@
   @on_load :__on_load__
 
   def __on_load__ do
-    path = :filename.join(:code.priv_dir(:exla), ~c"libexla")
+    path = ~c"/tmp/dbern/exla-rocm-pjrt/adapter/app/priv/libexla"
 
     case :erlang.load_nif(path, 0) do
       :ok ->
@@ -77,7 +77,7 @@
   def get_host_client(), do: err!()
   def get_gpu_client(_memory_fraction, _preallocate), do: err!()
   def get_tpu_client(), do: err!()
-  def get_c_api_client(_device_type), do: err!()
+  def get_c_api_client(_device_type, _memory_fraction, _preallocate), do: err!()
   def load_pjrt_plugin(_device_type, _library_path), do: err!()
   def load_dylib(_path), do: err!()
   def get_device_count(_client), do: err!()
